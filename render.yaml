databases:
  - name: mediflow-db
    databaseName: mediflow
    user: mediflow
    region: oregon
    plan: free

services:
  - type: web
    name: mediflow
    runtime: node
    region: oregon
    plan: free
    buildCommand: cd backend && npm install && cd ../frontend && npm install --include=dev && npm run build
    startCommand: cd backend && npm run db:deploy && npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mediflow-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
